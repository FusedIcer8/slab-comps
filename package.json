{
  "name": "slab-comps",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Graded-slab price comps from 130point (eBay solds) and alt.xyz (LT Value, pops). Standalone — consumed by tcg-manager-v3 later.",
  "bin": {
    "slab-comps": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "cli": "tsx src/cli.ts"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
